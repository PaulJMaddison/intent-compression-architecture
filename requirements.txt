# Archive artefact/evaluation tooling (intentionally loose for quick setup).
# Package/runtime and test dependencies are declared in pyproject.toml.
# Use requirements.lock for pinned versions of this artefact toolchain.
pandas
tiktoken
jsonschema
python-docx
Pillow
PyMuPDF
